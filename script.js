const cache = () => {
    
}

const setup = () => {
    
}

const bind = () => {
    
}

const main = () => {
    cache();
    setup();
    bind();
}

window.addEventListener('DOMContentLoaded', main)